---
name: onboard-repo
description: Team-specific dev environment setup. Runs after onboard-structure and onboard-plugins as part of /onboard.
---

# Repo Dev Environment Setup

Team-specific setup steps for this repository. Add steps here as your team identifies
common first-time setup needs.

## Placeholder

No custom onboarding steps are defined yet.

If you need to add steps (e.g. install dependencies, set up credentials, run a setup script),
document them here and they will be run automatically as part of `/onboard`.
