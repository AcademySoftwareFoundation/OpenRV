# Git

- Commit messages: imperative mood, concise (e.g., "Fix login timeout bug")
- Keep commits focused — one logical change per commit
- PR titles should describe the user-facing change
- Keep PRs focused — one concern per PR
